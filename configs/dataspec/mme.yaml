defaults:
  - default
  - preprocessing: preprocess
  - evaluation: eval_mme

preprocessing:
  method:
    map:
      function:
        template: "{}"
    set_transform:
      transform:
        image_root: ${run.mme_image_root}
        extension: ""
